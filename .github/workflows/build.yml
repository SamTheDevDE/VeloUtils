name: Build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
      - uses: gradle/actions/setup-gradle@v6
      - name: Verify
        run: ./gradlew --no-daemon --configuration-cache clean qualityGate
      - uses: actions/upload-artifact@v7
        with:
          name: veloutils-${{ github.sha }}
          if-no-files-found: error
          path: |
            veloutils-api/build/libs/veloutils-api-*.jar
            veloutils-proxy/build/libs/VeloUtils-Velocity-*.jar
            veloutils-bridge/build/libs/VeloUtils-Paper-*.jar
